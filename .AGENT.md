# .AGENT.md — dsh-ecosystem 伞仓代理工作手册

> 给在此仓工作的 AI 代理与协作者的守则。开始任何会话前:先读 `README.md` 与本文;
> 涉及路线图 / 里程碑先读 `docs/ECOSYSTEM-PLAN.md`;每次实质工作落盘后更新 `docs/WORKLOG.md`。

## 1. 这是什么

**伞仓(umbrella repo)**:以 [dsh-launcher](https://github.com/kuaizhongqiang/dsh-launcher) 为核心的 DeepSeek Harness(dsh) 个人生态的**版本锁仓**。
本仓**不承载实现代码**,只含三类东西:

1. 6 个 **git 子模块**的 commit 指针(gitlink)= 生态各组件的版本锁;
2. `docs/` = 路线图 / 审查 / 工作日志的**单一事实源**;
3. 根级元数据:`README.md`、`LICENSE`、`.gitmodules`、本文。

实现代码在各自子模块仓库中推进;本地开发目录在 `F:\Project\dsh-dev\*`(见 WORKLOG)。

## 2. 子模块纪律(最重要,违反会弄脏版本锁)

| 子模块 | 生态位 | 说明 |
|---|---|---|
| `dsh-launcher` | L0 载体 | 伞仓核心(单文件安装 + 启动引导) |
| `dsh-plugins` | L3 插件 | **开发集合视角**(11 包 + install-* 技能,路线图 §8 计划合并为 7) |
| `dsh-vscode` / `dsh-desktop` | L4 周边 | VSCode 扩展 / Electron 桌面壳 |
| `dsh-remote` | L6 广域网 | Cloudflare Access 远程部署记录 |
| `deepseek-harness` | L2 核心 | 官方上游 `deepseek-ai/deepseek-harness`,**只读消费,非自有仓** |

铁律:

1. **本仓默认不初始化子模块目录**——工作树只含元数据(docs + gitlink)。
   `git status` 里 6 个子模块显示 `deleted:` 是**预期状态**(gitlink 仍在 index,目录未检出),
   **绝不要**用 `git add -A` / `add: all` 把它们提交成「删除子模块」。
2. 需要子模块内容时按需 `git submodule update --init <name>`(或 `--init --recursive` 全量)。
3. 子模块内部改动在**各子仓**独立提交/推送;伞仓只做 **bump 指针**:
   ```powershell
   git submodule update --remote   # 谨慎:把各子模块推到各自远端 HEAD
   git add <submodule> && git commit -m "chore: bump <name> → <sha 前 8>"
   ```
   推荐**手动逐仓验证后再 bump**,保持伞仓指针 = 已验证版本(README「指针更新」同述)。
4. **`dsh-launcher` 内部另有一个自己的 `dsh-plugins` 子模块**(安装/拉取时的**运行时源**),
   与伞仓顶层 `dsh-plugins`(**开发集合视角**)两处指针**独立 bump**;更新插件时两处都要推进。
5. 敏感红线(永不进本仓或任何同步/清单):`.credentials.yaml`、`launch-token.json`、
   `connections.json`(各组 token)、`sessions/` 等凭证/运行时文件(PLAN D2 / §1)。

## 3. 文档索引(单一事实源)

| 文件 | 内容 | 状态 |
|---|---|---|
| `docs/ECOSYSTEM-PLAN.md` | 生态化路线图 **v3**:分层 L0–L6、决策 D1–D8、Phase 1–8、里程碑 M0–M8 / PM1–PM4、§8 插件 11→7 合并 | **生效版** |
| `docs/ECOSYSTEM-PLAN-REVIEW.md` | 初轮审查(18 条,已消化) | 存档 |
| `docs/ECOSYSTEM-PLAN-REVIEW-DEEP.md` | 深度审查(代码级 + 实测,有效 8 条已进 v3) | 存档 |
| `docs/WORKLOG.md` | 工作日志 / 交接——**每次会话必更** | 活文档 |
| `README.md` | 仓门面:组件清单 / 一条命令拉全生态 / 指针更新 | — |

里程碑 issue 集中在 **dsh-ecosystem Issues**:
M0=#3(dsh-launcher#1 UI transfer 原档)、M1–M8=#5–#12、PM1–PM4=#13–#16;
原 dsh-launcher / dsh-plugins 里程碑已关闭并留跳转注释,不再维护。

## 4. 术语速查

- **层**:L0 载体(launcher.exe)/ L1 运行时(Node ^22.19 ‖ >=24)/ L2 核心(dsh)/ L3 插件 /
  L4 周边(desktop·vscode)/ L5 个人层(`%DSH_HOME%`)/ L6 广域网连接。
- **决策 D1–D8**(PLAN §3):D1 launcher 不手写 DSH_HOME(只编排 install.ps1)·
  D2 凭证红线(明文 token 仅存本地)· D3 清单即生态(`ecosystem.json`)· D4 目录边界不变 ·
  D5 连接即启动项(`connections.json` 多组,兼容写 v1 `launch-token.json`)·
  D6 重启委托管理者(发现链:`DSH_LAUNCHER_EXE` 环境变量 → `%DSH_HOME%\launcher-registration.json`)·
  D7 插件按凭证/模式聚合(不按工具拆分)· D8 监督者协调协议(clearLaunchToken 原子化、端口锁、active 变更标记、原子写)。
- **关键文件**:`ecosystem.json`(M1 生态清单 + sha256 供应链校验)、
  `%DSH_HOME%\connections.json`(多连接 + token,仅本地)、`%DSH_HOME%\launcher-registration.json`(重启 seam,含心跳)、
  launcher 旁 `ecosystem-state.json`。
- **插件合并目标 11→7**:dsh-media / dsh-deepseek / dsh-credentials / **dsh-github** / dsh-stock /
  dsh-unity / dsh-launcher(新增 5 工具 + install-launcher 技能)。
- 审查优先级代号:P0(必修)/ P1(必补)/ P2(采纳)/ P3(酌情)。

## 5. 本仓改动规约

1. 只提交 `docs/` 与根级元数据文件(`README.md`、`.AGENT.md`、`.gitmodules`、`LICENSE`)。
2. 与路线图相关的改动先核对 PLAN 里程碑表;建/改 issue 用上文编号映射,勿与原仓 issue 混淆。
3. 实质工作落盘后更新 `docs/WORKLOG.md`(按日期段追加,沿用现格式,标注「已完成 / 下一步 / 文件清单」)。
4. 提交信息沿用现风格:`docs: …` / `chore: …` 前缀 + 一行摘要。
5. 语言:中文为主,术语/文件名词保留英文原文。

## 6. 给 AI 代理的操作提示

- 远端 = `kuaizhongqiang/dsh-ecosystem`,默认分支 `main`,直接推 main 是本仓既定工作流(无 CI/PR 门)。
- GitHub 操作走已配置的 **GITHUB_TOKEN**(fine-grained,source=file);工具层可用
  `github_sync` **以显式 path 指向本目录**(`F:\Project\dsh-ecosystem`)做 status / commit / push,
  无需另行克隆副本。
- 提交前看工作树:若脏状态仅为 6 个子模块 `deleted:`,commit 用**显式 pathspec**
  (如 `add: ".AGENT.md"`),**不要 `add -A` / `all`**,避免误提交子模块删除。
- 需要读子模块内容:`git submodule update --init <name>` 后按需读取;用完可保留目录
  (git status 会随之把该子模块显示为「修改/未跟踪」,属正常,勿提交)。
- 本仓零代码零测试:不需要构建/运行/测试步骤;所有工作产出都是文档与指针提交。
