# .AGENT.md — dsh-ecosystem 伞仓代理工作手册

> 给在此仓工作的 AI 代理与协作者的守则。开始任何会话前:先读 `README.md` 与本文;
> 涉及路线图 / 里程碑先读 `docs/ECOSYSTEM-PLAN.md`;每次实质工作落盘后更新 `docs/WORKLOG.md`。

## 1. 这是什么

**伞仓(umbrella repo)**:以 [dsh-launcher](https://github.com/kuaizhongqiang/dsh-launcher) 为核心的
DeepSeek Harness(dsh)个人生态的**总工作区与文档仓**(形态改造见 `docs/RESTRUCTURE-UMBRELLA.md`,2026-09-04 执行)。
本仓 git 树本身**不承载实现代码**,只含:

1. `docs/` = 路线图 / 审查 / 工作日志 / 形态设计的**单一事实源**;
2. 根级元数据:`README.md`、`LICENSE`、`.gitmodules`、`.gitignore`、`.github/`、本文;
3. `deepseek-harness` 官方子模块的 commit 指针(gitlink)= dsh 本体的构建基线锁。

**5 个自有组件以直接工程检出在伞仓根目录**(`dsh-launcher/` `dsh-plugins/` `dsh-vscode/` `dsh-desktop/`
`dsh-remote/`):每个都是**独立 git 仓库**(自有 .git / remote / 历史),伞仓不跟踪其内容
(根 `.gitignore` 忽略)。改动在**各仓内**提交推送,伞仓只提交 docs 与元数据。

## 2. 直接工程与子模块纪律(最重要,违反会弄脏伞仓)

| 目录 | 形态 | 说明 |
|---|---|---|
| `dsh-launcher` | 直接工程(独立 git) | L0 载体(伞仓核心) |
| `dsh-plugins` | 直接工程(独立 git) | L3 插件合集(7 包 + install-* 技能) |
| `dsh-vscode` / `dsh-desktop` | 直接工程(独立 git) | L4 周边(VSCode 扩展 / Electron 桌面壳) |
| `dsh-remote` | 直接工程(独立 git) | L6 广域网(Cloudflare Access 部署记录) |
| `deepseek-harness` | **git 子模块** | L2 核心,官方上游 `deepseek-ai/deepseek-harness`,只读消费 |

铁律:

1. **直接工程目录各自独立提交**:涉及组件代码的改动 → `cd <dir>`,在**各仓内** add/commit/push 到各自 origin;
   伞仓 git 的操作**永远用显式 pathspec**(`add: "docs/..."` 等)。
2. **严禁对 5 个直接工程目录 `git add`**(包括 `add -A` / `all`)——`.gitignore` 已兜底忽略,
   但显式 `git add dsh-launcher/…` 仍可强制添加,绝不做。
3. **`deepseek-harness` 是唯一 git 子模块**:默认未检出(gitlink 在 index,目录为空 = 预期)。
   需要内容时 `git submodule update --init deepseek-harness`;官方推进后人工验证再 bump:
   `git add deepseek-harness && git commit -m "chore: bump deepseek-harness → <sha 前 8>"`。
   **不做** `git submodule update --remote` 的自动推进。
4. **注意双 dsh-plugins**:`dsh-launcher` 仓内另有自己的 `dsh-plugins` 子模块(安装/拉取时的**运行时源**),
   属 launcher 内部结构(在 launcher 仓的 .gitmodules 里),与伞仓根的 `dsh-plugins` 直接工程目录
   相互独立——更新插件时:伞仓根的目录直接 `git pull` + 推进;launcher 内层按其仓纪律处理,勿混淆。
5. 敏感红线(永不进本仓或任何同步/清单):`.credentials.yaml`、`launch-token.json`、
   `connections.json`(各组 token)、`sessions/` 等凭证/运行时文件(PLAN D2 / §1)。

## 3. 文档索引(单一事实源)

| 文件 | 内容 | 状态 |
|---|---|---|
| `docs/ECOSYSTEM-PLAN.md` | 生态化路线图 **v3**:分层 L0–L6、决策 D1–D8、Phase 1–8、里程碑 M0–M8 / PM1–PM4、§8 插件合并 7 包 | **生效版** |
| `docs/RESTRUCTURE-UMBRELLA.md` | **伞仓形态改造设计**(直接工程 + harness 唯一 submodule、D-R1–R5、迁移步骤) | **已执行**(2026-09-04) |
| `docs/ECOSYSTEM-PLAN-REVIEW.md` | 初轮审查(18 条,已消化) | 存档 |
| `docs/ECOSYSTEM-PLAN-REVIEW-DEEP.md` | 深度审查(代码级 + 实测,有效 8 条已进 v3) | 存档 |
| `docs/WORKLOG.md` | 工作日志 / 交接——**每次会话必更** | 活文档 |
| `docs/modules/` | 各组件生态位说明书(索引 + 6 页:5 直接工程 HEAD 快照 + harness 子模块指针) | 活文档 |
| `docs/RELEASING.md` | 伞仓发布流程(tag 即生态汇总快照) | 活文档 |
| `.github/` | Issue/PR 模板(bug / feature / 里程碑任务)+ release notes 模板 | 治理 |
| `README.md` | 仓门面:组件清单 / 拉取工作区 / 版本状态 | — |

里程碑 issue 集中在 **dsh-ecosystem Issues**:
M0=#3(dsh-launcher#1 UI transfer 原档)、M1–M8=#5–#12、PM1–PM4=#13–#16;
原 dsh-launcher / dsh-plugins 里程碑已关闭并留跳转注释,不再维护。

## 4. 术语速查

- **层**:L0 载体(launcher.exe)/ L1 运行时(Node ^22.19 ‖ >=24)/ L2 核心(dsh)/ L3 插件 /
  L4 周边(desktop·vscode)/ L5 个人层(`%DSH_HOME%`)/ L6 广域网连接。
- **决策 D1–D8**(PLAN §3):D1 launcher 不手写 DSH_HOME(只编排 install.ps1)·
  D2 凭证红线(明文 token 仅存本地)· D3 清单即生态(`ecosystem.json`)· D4 目录边界不变 ·
  D5 连接即启动项(`connections.json` 多组,兼容写 v1 `launch-token.json`)·
  D6 重启委托管理者(发现链:`DSH_LAUNCHER_EXE` 环境变量 → `%DSH_HOME%\launcher-registration.json`)·
  D7 插件按凭证/模式聚合(不按工具拆分)· D8 监督者协调协议(clearLaunchToken 原子化、端口锁、active 变更标记、原子写)。
- **形态决策 D-R1–R5**(RESTRUCTURE 文档 §3):直接工程形态 / 版本锁=各仓远端 HEAD /
  逐仓 clone 拉取 / harness 唯一 submodule / 伞仓 release 简化。
- **关键文件**(launcher 侧运行时):`ecosystem.json`(M1 生态清单 + sha256 供应链校验)、
  `%DSH_HOME%\connections.json`(多连接 + token,仅本地)、`%DSH_HOME%\launcher-registration.json`(重启 seam,含心跳)、
  launcher 旁 `ecosystem-state.json`。
- **插件合集(已 7 包)**:dsh-media / dsh-deepseek / dsh-credentials / dsh-github / dsh-stock /
  dsh-unity / dsh-launcher。
- 审查优先级代号:P0(必修)/ P1(必补)/ P2(采纳)/ P3(酌情)。

## 5. 本仓改动规约

1. 只提交 `docs/`、`.github/` 与根级元数据文件(`README.md`、`.AGENT.md`、`.gitmodules`、`.gitignore`、`LICENSE`)。
2. 与路线图相关的改动先核对 PLAN 里程碑表;建/改 issue 用上文编号映射,勿与原仓 issue 混淆。
3. 实质工作落盘后更新 `docs/WORKLOG.md`(按日期段追加,沿用现格式,标注「已完成 / 下一步 / 文件清单」)。
4. 自有组件版本有推进时,顺手刷新 `docs/modules/` 对应页的 HEAD 快照与 WORKLOG(非 bump 仪式,保持可读)。
5. 提交信息沿用现风格:`docs: …` / `chore: …` 前缀 + 一行摘要。
6. 语言:中文为主,术语/文件名词保留英文原文。

## 6. 给 AI 代理的操作提示

- 远端 = `kuaizhongqiang/dsh-ecosystem`,默认分支 `main`,直接推 main 是本仓既定工作流(无 CI/PR 门)。
- GitHub 操作走已配置的 **GITHUB_TOKEN**(fine-grained,source=file);工具层可用
  `github_sync` **以显式 path 指向本目录**做 status / commit / push,或直接用本地 git。
- 提交前看工作树:正常应**干净**(5 直接工程目录被忽略不显示;harness 目录为空且 gitlink 未初始化,
  不构成脏改动);若出现脏状态,commit 一律用**显式 pathspec**,不要 `add -A` / `all`。
- 涉及组件代码的改动:进入对应目录按**各仓**规范提交推送;伞仓在其版本变化后更新模块页快照与 WORKLOG。
- 需要 dsh 本体源码:`git submodule update --init deepseek-harness`,用完可保留(harness 检出不纳入伞仓提交)。
- 本仓零代码零测试:不需要构建/运行/测试步骤;伞仓产出是文档与元数据提交。
