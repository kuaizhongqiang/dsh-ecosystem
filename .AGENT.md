# .AGENT.md — dsh-ecosystem 伞仓代理工作手册(monorepo)

> 给在此仓工作的 AI 代理与协作者的守则。开始任何会话前:先读 `README.md` 与本文;
> 涉及路线图 / 里程碑先读 `docs/ECOSYSTEM-PLAN.md`;涉及发布先读 `docs/RELEASING.md`;
> 每次实质工作落盘后更新 `docs/WORKLOG.md`。

## 1. 这是什么

**伞仓 = dsh 个人生态的 monorepo 与唯一权威代码/发布仓**(2026-09-04 收敛,见 `docs/MONOREPO-UMBRELLA.md`)。
本仓 git 树包含:

1. `docs/` = 路线图 / 形态设计 / 审查 / 工作日志的**单一事实源**;
2. **4 个自有组件目录**(`dsh-launcher/` `dsh-plugins/` `dsh-vscode/` `dsh-desktop/`)
   = 各组件全部代码(普通目录,随伞仓统一版本控制;`dsh-remote` 组件已移除,内容在已归档源仓);
3. `deepseek-harness` 官方子模块 gitlink = dsh 本体构建基线锁;
4. 根级元数据:`README.md`、`LICENSE`、`.gitmodules`、`.gitignore`、`scripts/`、`.github/`、本文;
5. `.github/workflows/release.yml` = **全量发布 CI**(push tag `v*` 触发,见 RELEASING.md)。

## 2. 纪律(最重要)

| 目录 | 形态 | 说明 |
|---|---|---|
| `dsh-launcher/` 等 4 目录 | **伞仓普通目录** | 组件代码在此,改动直接在伞仓 git 提交 |
| `deepseek-harness/` | **唯一 git 子模块** | L2 核心,官方 `deepseek-ai/deepseek-harness`,只读消费 |

铁律:

1. **组件代码在伞仓统一提交**:所有改动(组件或 docs)都 `git add <显式路径>` + commit 在本仓;
   **没有逐仓 remote**(源仓已归档只读)。提交信息:`feat:` / `fix:` / `docs:` / `chore:` / `release:`。
2. **禁止引入嵌套 git 元数据**:组件目录内不得出现 `.git` / `.gitmodules`;launcher 的插件源清单
   (`dsh-launcher/ecosystem.json` 与 `src/ecosystem.ts` 内嵌)指向 **kuaizhongqiang/dsh-ecosystem** +
   伞仓 commit(不是已归档的 dsh-plugins 仓)。插件集内容更新必须**重算 sha + 同步两处清单** +
   `commit` 指向含该内容的伞仓提交(见 RELEASING.md 发布前置)。
3. **`deepseek-harness` 是唯一 git 子模块**:默认未检出(gitlink 在 index,目录为空 = 预期)。
   需要内容时 `git submodule update --init deepseek-harness`;官方推进后人工验证再 bump:
   `git add deepseek-harness && git commit -m "chore: bump deepseek-harness → <sha 前 8>"`。
   **不做** `git submodule update --remote` 自动推进。
4. **CI 只在伞仓根生效**:`.github/workflows/release.yml` 是活的发布管道;
   组件目录内的 `.github/workflows/*` 是**归档死文件**(源仓遗物,GitHub 只认仓库根 workflows,
   已随归档停摆)——**勿改、勿删、勿当成活 CI**。
5. **质量门在各自组件目录内跑**:launcher(`npm run check` / `verify:m*`)、plugins(`scripts/verify-*.mjs`)、
   vscode(`pnpm typecheck` / `pnpm test`)、desktop(desktop/ 下 build + smoke);
   伞仓根 `scripts/verify-release.mjs` = 发布前清单一致性门(本地与 CI 共用)。
6. 敏感红线(永不进仓或任何同步/清单):`.credentials.yaml`、`launch-token.json`、
   `connections.json`(各组 token)、`sessions/` 等凭证/运行时文件(PLAN D2 / §1)。
   组件内 `.env.example` 允许,真实 `.env` 禁止。

## 3. 文档索引(单一事实源)

| 文件 | 内容 | 状态 |
|---|---|---|
| `docs/ECOSYSTEM-PLAN.md` | 生态化路线图 **v3**:分层 L0–L6、决策 D1–D8、Phase 1–8、里程碑 M0–M8 / PM1–PM4 | **生效版** |
| `docs/MONOREPO-UMBRELLA.md` | 伞仓 monorepo 化设计(D-M1–M6、阶段 A/B) | 阶段 A 已执行 |
| `docs/RESTRUCTURE-UMBRELLA.md` | 直接工程形态设计(D-R1–R5) | 已执行,被取代 |
| `docs/ECOSYSTEM-PLAN-REVIEW.md` | 初轮审查(18 条,已消化) | 存档 |
| `docs/ECOSYSTEM-PLAN-REVIEW-DEEP.md` | 深度审查(有效 8 条已进 v3) | 存档 |
| `docs/WORKLOG.md` | 工作日志 / 交接——**每次会话必更** | 活文档 |
| `docs/modules/` | 各组件生态位说明书(索引 + 5 页:4 自有 + harness) | 活文档 |
| `docs/RELEASING.md` | 发布流程(**全量 tag,自动化 CI**) | 活文档 |
| `.github/` | release.yml + Issue/PR 模板 + release notes 模板 | 治理 |
| `README.md` | 仓门面:结构 / 组件清单 / 拉取 / 发布 | — |

里程碑 issue 集中在 **dsh-ecosystem Issues**:
M0=#3(dsh-launcher#1 UI transfer 原档)、M1–M8=#5–#12、PM1–PM4=#13–#16;
原 dsh-launcher / dsh-plugins 里程碑已关闭并留跳转注释,不再维护。

## 4. 术语速查

- **层**:L0 载体(launcher)/ L1 运行时(Node ^22.19 ‖ >=24)/ L2 核心(dsh)/ L3 插件 /
  L4 周边(desktop·vscode)/ L5 个人层(`%DSH_HOME%`)/ L6 广域网连接(连接概念保留,部署记录组件已移除)。
- **决策 D1–D8**(PLAN §3):D1 launcher 不手写 DSH_HOME · D2 凭证红线 · D3 清单即生态 ·
  D4 目录边界不变 · D5 连接即启动项 · D6 重启委托管理者 · D7 插件按凭证/模式聚合 ·
  D8 监督者协调协议。
- **形态/发布决策**:D-M1–M6(MONOREPO §3);全量发布 = 一个 `vX.Y.Z` tag 发所有包(RELEASING)。
- **关键文件**(launcher 侧运行时):`dsh-launcher/ecosystem.json`(插件源清单:伞仓 repo + commit + sha256)、
  `src/ecosystem.ts`(内嵌同清单)、`%DSH_HOME%\connections.json`、`launcher-registration.json`(重启 seam)。
- **插件合集(7 包)**:dsh-media / dsh-deepseek / dsh-credentials / dsh-github / dsh-stock /
  dsh-unity / dsh-launcher。
- 审查优先级代号:P0(必修)/ P1(必补)/ P2(采纳)/ P3(酌情)。

## 5. 本仓改动规约

1. 提交范围 = 全仓(组件代码 + docs + scripts + .github + 元数据),**显式 pathspec**,慎用 `add -A`。
2. 与路线图相关的改动先核对 PLAN 里程碑表;建/改 issue 用上文编号映射。
3. 实质工作落盘后更新 `docs/WORKLOG.md`(按日期段追加,标注「已完成 / 下一步 / 文件清单」)。
4. **发布相关改动**走 RELEASING.md 流程:版本 bump、插件清单 sha/commit 同步、`scripts/verify-release.mjs`。
5. 提交信息:`feat:` / `fix:` / `docs:` / `chore:` / `release:` 前缀 + 一行摘要。
6. 语言:中文为主,术语/文件名词保留英文原文。

## 6. 给 AI 代理的操作提示

- 远端 = `kuaizhongqiang/dsh-ecosystem`,默认分支 `main`,直接推 main 既定工作流(无 CI/PR 门)。
- GitHub 操作走 **GITHUB_TOKEN**(fine-grained)或本地 gh(`kuaizhongqiang`,repo+workflow scope);
  看发布 CI 用 `gh run list/watch/view --log --repo kuaizhongqiang/dsh-ecosystem`。
- 提交前看工作树:组件目录修改会正常显示;harness 目录空(未初始化)不构成脏改动。
- 涉及单组件改动:在目录内完成,提交与推送都在伞仓。
- 需要 dsh 本体源码:`git submodule update --init deepseek-harness`,用完可保留(不提交其检出)。
- 伞仓根无统一构建;验证组件改动 = 进入该组件目录跑其质量门;发布门 = `node scripts/verify-release.mjs`。
- 后续工作项备忘:组件内部旧文档对已归档仓/旧 submodule 的引用清扫;Open VSX/NPM 发布历史观察;
  launcher 安装旧版(0.7.x,运行时源为归档 dsh-plugins 仓)的用户升级路径说明。

## 7. 形态演进速记(避免混淆)

1. 2026-09-04 前:文档 + 6 gitlink 版本锁(子模块)。
2. 2026-09-04 直接工程:5 自有仓独立 git 检出在伞仓根(各自 remote)——RESTRUCTURE。
3. 2026-09-04 monorepo:5 组件并入伞仓单一 git,源仓归档只读,harness 唯一子模块——MONOREPO。
4. 2026-09-04 发布重做:`dsh-remote` 移除;全量 `vX.Y.Z` tag 发布(伞仓根 release.yml);
   launcher 运行时源(插件 + 自更新)切到伞仓;desktop updater feed 切到伞仓。
