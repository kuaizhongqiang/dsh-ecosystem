# .AGENT.md — dsh-ecosystem 伞仓代理工作手册(monorepo)

> 给在此仓工作的 AI 代理与协作者的守则。开始任何会话前:先读 `README.md` 与本文;
> 涉及路线图 / 里程碑先读 `docs/ECOSYSTEM-PLAN.md`;每次实质工作落盘后更新 `docs/WORKLOG.md`。

## 1. 这是什么

**伞仓 = dsh 个人生态的 monorepo 与唯一权威代码仓**(2026-09-04 收敛,见 `docs/MONOREPO-UMBRELLA.md`)。
本仓 git 树包含:

1. `docs/` = 路线图 / 形态设计 / 审查 / 工作日志的**单一事实源**;
2. **5 个自有组件目录**(`dsh-launcher/` `dsh-plugins/` `dsh-vscode/` `dsh-desktop/` `dsh-remote/`)
   = 各组件全部代码(普通目录,随伞仓统一版本控制;原源仓已并入并归档只读);
3. `deepseek-harness` 官方子模块 gitlink = dsh 本体构建基线锁;
4. 根级元数据:`README.md`、`LICENSE`、`.gitmodules`、`.gitignore`、`.github/`、本文。

## 2. 纪律(最重要)

| 目录 | 形态 | 说明 |
|---|---|---|
| `dsh-launcher/` 等 5 目录 | **伞仓普通目录** | 组件代码在此,改动直接在伞仓 git 提交 |
| `deepseek-harness/` | **唯一 git 子模块** | L2 核心,官方 `deepseek-ai/deepseek-harness`,只读消费 |

铁律:

1. **组件代码在伞仓统一提交**:所有改动(组件或 docs)都 `git add <显式路径>` + commit 在本仓;
   **不再有逐仓 remote 推送**(源仓已归档只读)。提交信息风格:`feat:` / `fix:` / `docs:` / `chore:`。
2. **禁止重新引入嵌套 git 元数据**:组件目录内不得出现 `.git` / `.gitmodules`(并入时已清除;
   launcher/desktop 原有内层 submodule 依赖已随 monorepo 化废弃——若组件代码仍引用「子模块」说明,
   属待清理的文档遗留,见 §6 后续项,不要恢复 .gitmodules)。
3. **`deepseek-harness` 是唯一 git 子模块**:默认未检出(gitlink 在 index,目录为空 = 预期)。
   需要内容时 `git submodule update --init deepseek-harness`;官方推进后人工验证再 bump:
   `git add deepseek-harness && git commit -m "chore: bump deepseek-harness → <sha 前 8>"`。
   **不做** `git submodule update --remote` 自动推进。
4. **组件自带的 `.github/workflows/*` 是归档文件、不生效**:GitHub Actions 只识别**仓库根**
   `.github/workflows`,嵌套在 `dsh-launcher/.github/…` 里的旧 CI 不会运行(源仓归档后已停摆)。
   伞仓重建 CI / 迁移这些工作流到根级 = 后续工作项(MONOREPO D-M2),**不要**直接改组件目录内的 workflows。
5. **质量门在各自组件目录内跑**:launcher(`npm run build` / `verify:m*`)、plugins(`scripts/verify-*.mjs`)、
   vscode(`pnpm test`)、desktop(其 scripts)——伞仓根**不建**统一构建/测试。
6. 敏感红线(永不进仓或任何同步/清单):`.credentials.yaml`、`launch-token.json`、
   `connections.json`(各组 token)、`sessions/` 等凭证/运行时文件(PLAN D2 / §1)。
   组件内 `.env.example` 允许,真实 `.env` 禁止。

## 3. 文档索引(单一事实源)

| 文件 | 内容 | 状态 |
|---|---|---|
| `docs/ECOSYSTEM-PLAN.md` | 生态化路线图 **v3**:分层 L0–L6、决策 D1–D8、Phase 1–8、里程碑 M0–M8 / PM1–PM4、§8 插件合并 7 包 | **生效版** |
| `docs/MONOREPO-UMBRELLA.md` | **伞仓 monorepo 化设计**(D-M1–M6、阶段 A/B) | 阶段 A 已执行,阶段 B(归档/手动 SOP)进行中 |
| `docs/RESTRUCTURE-UMBRELLA.md` | 直接工程形态设计(D-R1–R5) | 已执行,**被 MONOREPO 取代** |
| `docs/ECOSYSTEM-PLAN-REVIEW.md` | 初轮审查(18 条,已消化) | 存档 |
| `docs/ECOSYSTEM-PLAN-REVIEW-DEEP.md` | 深度审查(代码级 + 实测,有效 8 条已进 v3) | 存档 |
| `docs/WORKLOG.md` | 工作日志 / 交接——**每次会话必更** | 活文档 |
| `docs/modules/` | 各组件生态位说明书(索引 + 6 页) | 活文档 |
| `docs/RELEASING.md` | 发布流程(**手动 SOP**,组件发版本地构建 + 手动上传) | 活文档 |
| `.github/` | Issue/PR 模板 + release notes 模板 | 治理 |
| `README.md` | 仓门面:结构 / 组件清单 / 拉取 / 版本状态 | — |

里程碑 issue 集中在 **dsh-ecosystem Issues**:
M0=#3(dsh-launcher#1 UI transfer 原档)、M1–M8=#5–#12、PM1–PM4=#13–#16;
原 dsh-launcher / dsh-plugins 里程碑已关闭并留跳转注释,不再维护。

## 4. 术语速查

- **层**:L0 载体(launcher)/ L1 运行时(Node ^22.19 ‖ >=24)/ L2 核心(dsh)/ L3 插件 /
  L4 周边(desktop·vscode)/ L5 个人层(`%DSH_HOME%`)/ L6 广域网连接。
- **决策 D1–D8**(PLAN §3):D1 launcher 不手写 DSH_HOME · D2 凭证红线 · D3 清单即生态 ·
  D4 目录边界不变 · D5 连接即启动项 · D6 重启委托管理者 · D7 插件按凭证/模式聚合 ·
  D8 监督者协调协议。
- **形态决策 D-M1–M6**(MONOREPO §3):并入快照 / 手动发版 SOP / harness 唯一子模块 /
  组件内嵌套 submodule 清除 / 发版语义 / 回滚禁 force push。
- **关键文件**(launcher 侧运行时):`ecosystem.json`(M1 生态清单 + sha256)、
  `%DSH_HOME%\connections.json`、`%DSH_HOME%\launcher-registration.json`(重启 seam)、`ecosystem-state.json`。
- **插件合集(7 包)**:dsh-media / dsh-deepseek / dsh-credentials / dsh-github / dsh-stock /
  dsh-unity / dsh-launcher。
- 审查优先级代号:P0(必修)/ P1(必补)/ P2(采纳)/ P3(酌情)。

## 5. 本仓改动规约

1. 提交范围 = 全仓(组件代码 + docs + 元数据),**显式 pathspec** 提交,慎用 `add -A`(harness 未检出
   目录为空无影响,但保持显式习惯)。
2. 与路线图相关的改动先核对 PLAN 里程碑表;建/改 issue 用上文编号映射。
3. 实质工作落盘后更新 `docs/WORKLOG.md`(按日期段追加,标注「已完成 / 下一步 / 文件清单」)。
4. 组件版本有推进时,同步 bump 组件目录内版本号并刷新 `docs/modules/` 对应页快照。
5. 提交信息:`feat:` / `fix:` / `docs:` / `chore:` 前缀 + 一行摘要。
6. 语言:中文为主,术语/文件名词保留英文原文。

## 6. 给 AI 代理的操作提示

- 远端 = `kuaizhongqiang/dsh-ecosystem`,默认分支 `main`,直接推 main 既定工作流(无 CI/PR 门)。
- GitHub 操作走 **GITHUB_TOKEN**(fine-grained);`github_sync` 以显式 path 指向本目录,或本地 git。
- 提交前看工作树:组件目录修改会正常显示(已是伞仓跟踪文件);harness 目录空(未初始化)不构成脏改动。
- 涉及单组件改动:在该目录内完成代码修改,但**提交与推送都在伞仓**(无逐仓 git)。
- 需要 dsh 本体源码:`git submodule update --init deepseek-harness`,用完可保留(harness 检出不提交)。
- 伞仓根无统一构建;验证组件改动 = 进入该组件目录跑其质量门。
- 后续工作项备忘:伞仓根重建 CI/Open VSX 管道(D-M2(b))、组件内部旧 submodule 文档清扫、
  源仓归档动作(用户在 GitHub UI,阶段 B)。

## 7. 形态演进速记(避免混淆)

1. 2026-09-04 前:文档 + 6 gitlink 版本锁(子模块)。
2. 2026-09-04 直接工程:5 自有仓独立 git 检出在伞仓根(各自 remote)——RESTRUCTURE。
3. 2026-09-04 monorepo(当前):5 组件并入伞仓单一 git,源仓归档只读,harness 唯一子模块——MONOREPO。
