# dsh-image 凭证模板(ARK_API_KEY)
#
# generate_image 按 credentials seam → 环境变量 顺序解析,任一可用即可:
#   方式 A(推荐):把下面的行合并进 %DSH_HOME%\.credentials.yaml(不要整文件复制):
#     ARK_API_KEY: 粘贴你的火山方舟 API Key
#   方式 B:环境变量  $env:ARK_API_KEY = "..."   或  setx ARK_API_KEY "..."
#
# 取值:火山方舟控制台 → API Key(https://console.volcengine.com/ark)。
# 该 Key 需能访问 Doubao Seedream 图片生成模型(默认 doubao-seedream-5-0-260128)。
#
# 走中转/OpenAI 兼容网关时不必新申请 Key:在 cordis.patch.yml 的 tool-image-gen 条目里
# 覆盖 baseURL(如 https://<网关>/v1)、imageField: images、apiKeyEnv: <网关那把 key 名>。
#
# 红线:不要把真实 key 提交进任何仓库或包文件。
# ARK_API_KEY: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
